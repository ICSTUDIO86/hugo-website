# 🚀 Public文件夹自动部署指南

## 📋 概述

现在有3个脚本可以将public文件夹内容直接推送到GitHub Pages：

```
📁 项目根目录
├── push-public.sh                 # ⚡ 最简单 - 一键部署
├── deploy-public-auto.sh          # 🔧 完整版 - 详细输出  
└── public/
    └── deploy.sh                  # 📂 在public目录中运行
```

## 🎯 使用方法

### 方法1：一键部署（推荐）
```bash
# 在项目根目录运行
./push-public.sh
```

### 方法2：完整版部署
```bash
# 在项目根目录运行，查看详细过程
./deploy-public-auto.sh
```

### 方法3：在public目录部署
```bash
# 进入public目录
cd public
./deploy.sh
```

## ⚡ 工作流程

1. **Hugo构建** → 生成/更新public文件夹
2. **运行脚本** → 自动推送到GitHub
3. **访问网站** → https://icstudio.club

## 📊 脚本功能

### ✅ 自动化特性：
- 🔍 检查public目录和文件
- 🌐 克隆GitHub仓库到临时目录  
- 🗑️ 清空仓库现有内容
- 📂 复制public文件夹所有内容
- 💾 自动提交和推送
- 🧹 清理临时文件

### 🛡️ 安全特性：
- ✅ 使用临时目录，不影响本地文件
- ✅ 保留GitHub仓库的.git历史
- ✅ 自动处理推送冲突（force-with-lease）
- ✅ 错误处理和清理机制

## 🎯 部署后的GitHub仓库结构

```
hugo-website/ (GitHub仓库)
├── index.html              # 主页
├── CNAME                   # 域名配置  
├── css/                    # 样式文件
├── js/                     # JavaScript文件
├── images/                 # 图片资源
├── sight-reading-tool/     # 工具页面
├── blog/                   # 博客内容
└── ...                     # 其他静态文件
```

## 🔧 故障排除

### SSH密钥问题
```bash
# 测试SSH连接
ssh -T git@github.com

# 如果失败，检查SSH密钥配置
```

### 权限问题
```bash
# 确保脚本有执行权限
chmod +x push-public.sh
chmod +x deploy-public-auto.sh
chmod +x public/deploy.sh
```

### public目录为空
```bash
# 先构建Hugo网站
hugo

# 然后运行部署脚本
./push-public.sh
```

## 🎉 成功标志

看到以下消息表示部署成功：
```
✅ 部署成功！
🌐 访问: https://icstudio.club  
🔗 GitHub: https://github.com/ICSTUDIO86/hugo-website
```

网站将在几分钟内更新！
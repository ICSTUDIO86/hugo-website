# Hugo网站部署指南

## 🚀 部署方式

### 方式一：自动部署（推荐）✅

使用GitHub Actions自动部署，最简单安全的方式：

```bash
# 1. 构建并提交更改
./deploy-simple.sh
```

这个脚本会：
- 自动构建Hugo网站
- 提交所有更改到git
- 推送到GitHub
- 触发GitHub Actions自动部署

### 方式二：手动部署

```bash
# 1. 构建网站
hugo --minify

# 2. 添加和提交更改
git add .
git commit -m "更新网站内容"

# 3. 推送到GitHub
git push origin main
```

## 📁 目录结构

```
pehtheme-hugo/
├── public/              # Hugo构建输出（自动生成）
├── content/            # 网站内容
├── static/             # 静态资源
├── layouts/            # Hugo模板
├── deploy-simple.sh    # 推荐的部署脚本 ✅
├── deploy-public-to-github.sh  # 高级部署脚本
└── push_to_github.sh   # 已废弃 ❌
```

## ⚠️ 重要说明

1. **不要手动编辑`public/`目录** - 这是Hugo自动生成的输出目录
2. **使用`deploy-simple.sh`** - 最简单可靠的部署方式
3. **避免使用`push_to_github.sh`** - 已废弃的旧脚本

## 🔧 故障排除

### 问题：推送失败
```bash
# 拉取远程更改并合并
git pull origin main
git push origin main
```

### 问题：构建失败
```bash
# 检查Hugo版本和配置
hugo version
hugo --minify --verbose
```

### 问题：权限错误
确保您有GitHub仓库的推送权限，或使用SSH密钥。

## 🌐 访问地址

- GitHub Pages: https://icstudio86.github.io/hugo-website/
- 自定义域名: https://icstudio.club

部署完成后，等待2-3分钟让GitHub Actions完成自动部署。